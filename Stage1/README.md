# Stage1

Stage1 now keeps the two project components separate:

- `vision_weather/`: camera-image weather classifier and its current default weight.
- `rain_retrieval/`: satellite-link rainfall retrieval model, data builders, analysis scripts, and workflow docs.
