
This folder is for implementing rainfall time series prediction, using GPT-2 as the main model for training. The input step size is 512, the output step sizes are 96, 192, and 512, and the step size scale is 10 minutes。
This folder contains training and evaluation bash files,  running with "scripts/train_weather.sh"
